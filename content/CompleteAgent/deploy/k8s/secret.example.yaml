# Replace placeholders with real values and apply with `kubectl apply -f`.
# Better: use Azure Key Vault CSI driver, External Secrets, or Sealed Secrets — do NOT commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: completeagent-secrets
  namespace: completeagent
type: Opaque
stringData:
  COMPLETEAGENT_Authentication__ApiKey: "REPLACE_WITH_RANDOM_32_CHAR_KEY"
  COMPLETEAGENT_Agent__AzureOpenAI__Endpoint: "https://your-resource.openai.azure.com"
  COMPLETEAGENT_RateLimit__RedisConnectionString: "redis-master:6379"
