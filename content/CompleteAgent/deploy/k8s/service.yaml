apiVersion: v1
kind: Service
metadata:
  name: completeagent
  namespace: completeagent
  labels:
    app: completeagent
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: completeagent
